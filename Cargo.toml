[package]
name = "spelletje-mac"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = { version = "0.4", optional = true }
termion = "4"

[features]
default = ["gui"]
gui = ["dep:macroquad"]
cli = []
